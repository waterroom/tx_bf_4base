参考https://github.com/waterroom/tx_bf仓库，设计一个4路300MHz基带数据分别过4个8通道宽带 TX 数字波束合成后上采样到2.4GHz采样率，
然后经过4个上变频模块后，每个通道对应加和四路数据的数字波束发射模块。
最终将在两片fpga上使用这个模块，每个fpga输出到8路DAC，最终形成最多四个指向四个不同频率下的16元波束形成发射信号；
先用matlab仿真再实现fpga代码；
工作目录：C:\workbuddy_chat\tx_bf_4base；
用git管理仓库；
fpga处理时钟300MHz；
要求除tb仿真文件外其他源文件必须用可综合的、时序友好的sv语法实现;
fifo和ram使用xilinx xpm和适合自推断的写法以满足可移植性，fir和dds使用xilinx IP核节省资源;
8倍内插滤波器系数放在了C:\workbuddy_chat\tx_bf_4base\rtl文件里
所有复位都用同步释放,所有复位都用高有效;
注释使用中文;
整个设计与实现过程不要修改工作目录外的文件，可以读取、复制到工作目录；
请仔细审阅用户的设计，用户的设计可能不是最优设计；
先做出详细的设计方案,具体到模块划分与接口设计;
